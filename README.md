# Pulse

Self-hosted API observability platform. Full README (architecture, quickstart, API
reference, benchmark numbers, design decisions) lands in Phase 7.

## Status

Phase 0 — scaffold & infra.

```
docker compose up --build
curl http://localhost:8000/health
```
